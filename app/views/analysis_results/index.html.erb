<div class="flex flex-col items-center justify-center sm:px-4 sm:py-8 px-2 py-4 sm:space-y-10 space-y-5 w-full">
    <!-- タイトル -->
    <div class="text-center">
        <h2 class="text-3xl font-bold">今週の気分を振り返る</h2>
        <p class="text-sm text-gray-500"><%= @start_date.strftime("%-m/%-d") %> 〜 <%= @end_date.strftime("%-m/%-d") %></p>
    </div>

    <!-- ヒートマップ ＆ レーダーチャート -->
    <div class="flex items-start justify-center flex-col lg:flex-row gap-8 p-4 w-full max-w-6xl mx-auto">
        <div>
        <%= render "shared/heatmap" %>
        </div>
        <div class="lg:h-[500px] h-[400px] w-[500px] lg:w-[600px]">
            <canvas id="myChart" class="rounded-lg shadow-lg p-4 neumorphism"></canvas>
        </div>
    </div>

    <!-- AIコメントエリア -->
    <div class="bg-white rounded-lg shadow p-6 space-y-4">
        <h3 class="text-xl font-semibold text-gray-700">AIからのコメント</h3>
        <p class="text-gray-600">
        「今週は“ホッと”が多く、穏やかな一週間でしたね。仕事や対人関係が安定していた可能性があります。」
        </p>
        <div class="flex gap-2">
        <button class="btn btn-primary">保存する</button>
        <button class="btn btn-outline">コピーする</button>
        </div>
    </div>
</div>
