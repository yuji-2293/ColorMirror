<div class="bg-gradient-diagonal flex items-start justify-between p-4 h-20">
    <%= link_to root_path, class: "hover:opacity-50" do %>
        <p class="maru text-4xl font-bold text-white">ColoMirror</p>
    <% end %>
    <% if user_signed_in? %>
        <%= render "shared/drawer_menu",class: "" %>
    <% else %>
        <%= render "shared/before_menu" %>
      <% end %>
</div>
