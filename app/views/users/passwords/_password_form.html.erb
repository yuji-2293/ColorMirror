<div class="flex items-center justify-center bg-gradient-diagonal rounded-xl shadow-lg sm:p-8 p-3">
  <div class="glass p-8 w-full max-w-md text-center">
    <h2 class="sm:text-3xl text-lg font-bold text-gray-800 mb-6">パスワードの再設定</h2>

    <%= form_with model: resource, url: password_path(resource_name) do |f| %>
      <div class="mb-4">
        <%= f.email_field :email, autofocus: true, autocomplete: "email",  class: "input-field p-2 w-full rounded-full focus:outline-none focus:shadow-md", placeholder: "メールアドレス" %>
      </div>
      <div class="sm:mt-4 mt-2">
        <%= f.submit "送信", class: "btn btn-white opacity-50 w-full py-3 sm:text-lg text:md" %>
      </div>
    <% end %>

    <div class="mt-4 text-sm">
      <p>パスワードを思い出しましたか？</p>
      <button type="button" id="return-button" class="btn btn-white">戻る</button>
    </div>
  </div>
</div>
